import 'package:flutter/material.dart';

class MoviesScrenn extends StatelessWidget {
  const MoviesScrenn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Filmes');
  }
}